services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: civicint
      POSTGRES_PASSWORD: civicint
      POSTGRES_DB: civicint
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
